---
version: "2.26.2"
date: "2026-08-13"
title: "Sync the UI kit to Claude Design"
summary: "The 28 primitives in src/components/ui are now importable inside Claude Design, so designs built there are made of the real SIMS components and map straight back onto shippable code. This release adds the reproducible sync inputs to the repo and stops the linter from walking generated build output."
new:
  - title: "SIMS component library published to Claude Design"
    detail: "All 139 exports across src/components/ui (Button, Card, Dialog, Select, Sidebar, Calendar, ChartContainer and their compound parts) are bundled and grouped into Actions, Forms, Overlays, Navigation, Layout and Data Display. Each component ships its real API contract, generated from the repo's own TypeScript, plus per-component usage docs. 31 components carry hand-authored preview cards using realistic merchant, ticket and lead content."
  - title: "Reproducible sync inputs under .design-sync/"
    detail: "config.json, conventions.md (the guidance the design agent reads before building), NOTES.md (repo-specific gotchas and re-sync risks), 31 authored preview files and the card-grouping stubs are all committed, so a future sync re-runs deterministically instead of rediscovering the setup."
improved: []
fixed:
  - title: "Lint no longer scans generated build output"
    detail: "eslint had no ignore entry for the design-sync build directories, so a run would walk the generated 1.5 MB component bundle and fail with 75 errors from third-party code. ds-bundle/, .ds-sync/ and .design-sync/ are now in the globalIgnores list."
breaking_changes: ""
---
