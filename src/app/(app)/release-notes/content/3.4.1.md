---
version: "3.4.1"
date: "2026-08-27"
title: "Tests for the authorization core"
summary: "The page-access mapping and the API access decision logic — the heart of the new authorization boundary — now have dedicated unit tests."
fixed:
  - title: "Authorization logic is unit-tested"
    detail: "The pure API access decision (evaluateApiAccess) moved into its own module so it can run under the plain Node test runner, and both it and the page-access route mappings (longest-prefix resolution, universal paths, legacy workspace grants, the Super Admin bypass) are now covered by tests. No behavior changed."
---
