---
version: "2.30.2"
date: "2026-08-27"
title: "Test suite back to green"
summary: "Two stale calendar test assertions and one over-specified test literal were fixed, so npm test and the TypeScript check both pass again — the prerequisite for gating releases on them."
fixed:
  - title: "Stale Google Calendar test expectations"
    detail: "Two calendar tests still asserted that the Google Maps link appears in the event description, a behavior that was deliberately removed when the link moved to the event's location field. The tests now guard the current behavior: the selected location goes in the event location, and the description never carries a Google Maps link."
  - title: "Ticket history actor test no longer over-specifies its input"
    detail: "The test passed name and email fields that the TicketHistoryActor type deliberately does not carry, which failed the TypeScript check. The test now passes only the id the function actually reads."
---
