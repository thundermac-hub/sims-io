---
version: "3.5.4"
date: "2026-08-29"
title: "Merchant Success analytics stops inspecting the database on every load"
summary: "Each visit to the analytics page ran an extra query asking whether the merchant sentiment column exists before it could build its report. A production survey confirmed the column is there, so the check is gone."
improved:
  - title: "One less query per analytics page load"
    detail: "The merchant sentiment breakdown was written defensively, checking the database's own catalogue for the column before every report it built — a leftover from when that column was still rolling out. The check was never cached, so it ran on every visit and every filter change. It has been confirmed present in production and the check removed."
---
