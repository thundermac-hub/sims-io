import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Merchant Success Ticket Categories",
}

export default function MerchantSuccessTicketCategoriesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
