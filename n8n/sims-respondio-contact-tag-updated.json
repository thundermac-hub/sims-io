{
  "name": "SIMS · Respond.io Contact Tag Updated",
  "nodes": [
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const isObj = (v) => !!v && typeof v === 'object' && !Array.isArray(v);\nconst str = (v) => {\n  if (typeof v === 'string' && v.trim()) return v.trim();\n  if (typeof v === 'number' && Number.isFinite(v)) return String(v);\n  return null;\n};\nconst first = (...vals) => {\n  for (const v of vals) { const s = str(v); if (s !== null) return s; }\n  return null;\n};\n\nconst body = isObj($json.body) ? $json.body : $json;\nconst contact = [body.contact, body.data, body.payload].find(isObj) || body;\nconst conversation = [body.conversation, contact.conversation].find(isObj) || {};\nconst assignee = [body.assignee, body.user, contact.assignee].find(isObj) || {};\n\nconst contactId = first(\n  contact.id, contact.contact_id, contact.contactId,\n  body.contact_id, body.contactId\n);\nif (!contactId) {\n  throw new Error('No Respond.io contact id in payload: ' + JSON.stringify(body).slice(0, 500));\n}\n\nconst contactName = first(\n  contact.name, contact.full_name, contact.fullName,\n  [str(contact.firstName || contact.first_name), str(contact.lastName || contact.last_name)]\n    .filter(Boolean).join(' ')\n);\n\n// Respond.io does not always send an event id. When it does not, SIMS builds its\n// idempotency key from event type + contact + timestamp + conversation instead, so a\n// stable timestamp matters more than a pretty one. Duplicate tickets are additionally\n// blocked server-side by the open-ticket guard in handleContactTagUpdated.\nconst eventId = first(body.event_id, body.eventId, body.id);\nconst occurredAt = first(body.occurred_at, body.occurredAt, body.timestamp, body.created_at, body.updated_at)\n  || new Date().toISOString();\n\nconst base = {\n  event_id: eventId,\n  occurred_at: occurredAt,\n  contact: {\n    id: contactId,\n    name: contactName,\n    email: first(contact.email, contact.contact_email),\n    phone: first(contact.phone, contact.phone_number, contact.phoneNumber),\n  },\n  conversation: {\n    conversation_id: first(conversation.id, conversation.conversation_id, body.conversation_id, body.conversationId),\n  },\n};\n\n// ---------------------------------------------------------------------------\n// EDIT ME: must match General > Integrations > \"Merchant Success routing tag\".\n// SIMS compares case-insensitively and ignores every other tag.\n// ---------------------------------------------------------------------------\nconst ROUTING_TAG = 'team:merchant_success';\n\n// Respond.io sends either a single tag or the contact's whole tag list, as strings or\n// as objects. Flatten both shapes, then prefer the routing tag if it is in there.\nconst tagSource = body.tag != null ? body.tag\n  : body.tags != null ? body.tags\n  : contact.tags != null ? contact.tags\n  : contact.tag;\nconst tagNames = (Array.isArray(tagSource) ? tagSource : [tagSource])\n  .map((t) => (isObj(t) ? first(t.name, t.tag, t.title, t.label) : str(t)))\n  .filter(Boolean);\nconst tag = tagNames.find((t) => t.toLowerCase() === ROUTING_TAG.trim().toLowerCase())\n  || tagNames[0]\n  || null;\n\nreturn { json: { ...base, event_type: 'contact.tag.updated', tag } };"
      },
      "id": "387b73b1-e4c0-407c-b1fd-640b6d4bf25c",
      "name": "Normalize for SIMS",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        448,
        0
      ],
      "notes": "Maps the Respond.io payload onto the field names POST /api/integrations/respond-io reads."
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://staging-sims.getslurp.com/api/integrations/respond-io",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json) }}",
        "options": {
          "response": {
            "response": {}
          },
          "timeout": 20000
        }
      },
      "id": "f17e1c9b-df59-433b-97c8-ffaadd8c7d1d",
      "name": "POST SIMS webhook",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        688,
        0
      ],
      "retryOnFail": true,
      "maxTries": 3,
      "waitBetweenTries": 5000,
      "credentials": {
        "httpHeaderAuth": {
          "id": "Fp3ndJbd1J0GAYCM",
          "name": "SIMS Webhook Secret - Staging"
        }
      },
      "notes": "Header Auth credential: name 'x-sims-webhook-secret', value = the secret issued in General > Integrations. Retries 3x, which is safe: the SIMS event ledger absorbs redelivery."
    },
    {
      "parameters": {
        "triggerEventType": "contactTagUpdated"
      },
      "type": "@respond-io/n8n-nodes-respond-io.respondioTrigger",
      "typeVersion": 1,
      "position": [
        32,
        0
      ],
      "id": "b7626545-35e3-4aa1-96aa-c0f457925040",
      "name": "Respond.io Trigger",
      "webhookId": "53f271e7-20ad-43fd-ab90-4026d0a249c4",
      "credentials": {
        "respondIoApi": {
          "id": "zdEmYw3z66uQ8GVS",
          "name": "Respond.io N8N API Key"
        }
      }
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 3
          },
          "conditions": [
            {
              "id": "1ca2e284-5ee5-44f0-93fa-21d2efc3eeb3",
              "leftValue": "tags",
              "rightValue": "",
              "operator": {
                "type": "string",
                "operation": "notEmpty",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.3,
      "position": [
        224,
        16
      ],
      "id": "eb29b921-8b58-47da-92ae-4d33f71e9284",
      "name": "If"
    }
  ],
  "pinData": {},
  "connections": {
    "Normalize for SIMS": {
      "main": [
        [
          {
            "node": "POST SIMS webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Respond.io Trigger": {
      "main": [
        [
          {
            "node": "If",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "If": {
      "main": [
        [
          {
            "node": "Normalize for SIMS",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false
  },
  "versionId": "de477788-c48f-4433-9b1a-bf49b76254f1",
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "7e3f2b6d5795f6ff9be6a209eb1d24dbfa8368e312712a7c1f50fff4dc8a068c"
  },
  "id": "4SdVBLvrCg1q9sxr",
  "tags": [
    {
      "updatedAt": "2026-08-18T06:53:18.306Z",
      "createdAt": "2026-08-18T06:53:18.306Z",
      "id": "5CkbJS7LkDj5bf0S",
      "name": "SIMS"
    }
  ]
}
